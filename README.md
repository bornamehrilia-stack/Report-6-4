# Report-6-4



---

# گزارش پروژه: ساخت سنسور دما و رطوبت با سنسور DHT11 (Temperature & Humidity Sensor with Arduino)

## ۱. مقدمه  
در این پروژه، یک **سنسور DHT11** به آردوینو متصل شده است و هم دمای محیط و هم رطوبت نسبی را اندازه‌گیری می‌کند. سپس این مقادیر به صورت عددی در Serial Monitor نمایش داده می‌شوند..

## ۲. اهداف پروژه  
- آشنایی با سنسور DHT11 و نحوه‌ی کار آن  
- یادگیری نحوه‌ی اتصال سنسور DHT11 به آردوینو  
- استفاده از کتابخانه‌ی `DHT.h` برای خواندن داده‌ها  
- نمایش دما و رطوبت در Serial Monitor  
- آشنایی با مفهوم “پروتکل دیجیتال” و “خواندن داده‌های سنسور”

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- سنسور DHT11  
- یک مقاومت ۴.۷k اهم (برای pull-up)  
- برد برد (Breadboard)  
- سیم‌های جامپر (jumper wires)

.

## ۴. نحوه‌ی ساخت مدار  
- **سنسور DHT11**:  
  - VCC → 5V آردوینو  
  - GND → GND آردوینو  
  - DATA → پین دیجیتال ۲ آردوینو  
  - یک مقاومت ۴.۷k اهم بین پین DATA و 5V قرار دهید (برای pull-up).

.

## ۵. کد برنامه (اسکچ):

```cpp
#include <DHT.h>   // اضافه کردن کتابخانه DHT

#define DHTPIN 2     // تعریف پین DATA
#define DHTTYPE DHT11 // تعریف نوع سنسور

DHT dht(DHTPIN, DHTTYPE); // ایجاد شیء DHT

void setup() {
  Serial.begin(9600);   // شروع ارتباط سریال برای نمایش داده
  dht.begin();          // شروع سنسور DHT
}

void loop() {
  delay(2000);  // تأخیر ۲ ثانیه برای ثبات کار

  float h = dht.readHumidity();        // خواندن رطوبت
  float t = dht.readTemperature();     // خواندن دما

  // بررسی خطا در خواندن داده‌ها
  if (isnan(h) || isnan(t)) {
    Serial.println("failed!");
    return;
  }

  Serial.print("humidity");             // نمایش متن
  Serial.print(h);                      // نمایش رطوبت
  Serial.print("% ");                   // نمایش واحد

  Serial.print("Temperature:");         // نمایش متن
  Serial.print(t);                      // نمایش دما
  Serial.println(" C");                 // نمایش واحد
}
```

### توضیح کد:  
- `#include <DHT.h>` — اضافه کردن کتابخانه‌ی DHT.  
- `#define DHTPIN 2` — تعریف پین DATA.  
- `#define DHTTYPE DHT11` — تعریف نوع سنسور.  
- `DHT dht(DHTPIN, DHTTYPE)` — ایجاد شیء DHT.  
- `dht.begin()` — شروع سنسور DHT.  
- `dht.readHumidity()` — خواندن رطوبت.  
- `dht.readTemperature()` — خواندن دما.  
- `if (isnan(h) || isnan(t))` — بررسی خطا در خواندن داده‌ها.  
- `Serial.print()` — نمایش داده‌ها در Serial Monitor.  
- `delay(2000)` — برای اینکه نمایش هر ۲ ثانیه یکبار به‌روز شود.

## ۶. نحوه‌ی کارکرد  
- پس از آپلود کد، سنسور شروع به اندازه‌گیری دما و رطوبت می‌کند.  
- دما و رطوبت به صورت عددی (به واحد سانتی‌گراد و درصد) در Serial Monitor نمایش داده می‌شوند.  
- اگر دست خود را نزدیک سنسور بگیرید، دما و رطوبت افزایش می‌یابند.

## ۷. نتایج  
سنسور به درستی دما و رطوبت را اندازه‌گیری و نمایش می‌دهد. .

> - کتابخانه‌ی `DHT.h` در محیط Arduino IDE نصب شده است.

اگر خواستید، می‌توانم نسخه‌ی انگلیسی آن را هم برایتان بنویسم یا فایل PDF از گزارش را آماده کنم. موفق باشید! 💡🌡️💧📡
